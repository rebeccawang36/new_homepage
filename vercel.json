{
  "version": 2,
  "builds": [
    { "src": "package.json", "use": "@vercel/node" }
  ],
  "installCommand": "pnpm install",
  "buildCommand": "pnpm build",
  "outputDirectory": "dist-server/public",
  "routes": [
    { "src": "/(.*)", "dest": "dist-server/public/$1" }
  ]
}
